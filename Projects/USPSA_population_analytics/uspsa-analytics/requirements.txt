# Core pipeline
playwright>=1.44        # after install, run once: playwright install chromium
SQLAlchemy>=2.0
pandas>=2.0
numpy>=1.26

# Dashboard
streamlit>=1.32
plotly>=5.18

# Optional: real PostgreSQL backend instead of the default SQLite file
# psycopg2-binary>=2.9
