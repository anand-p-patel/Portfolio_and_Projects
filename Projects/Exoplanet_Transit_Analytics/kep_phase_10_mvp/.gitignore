# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Streamlit secrets — never commit credentials
.streamlit/secrets.toml

# Scratch / local
*.log
*.bak
.DS_Store

# NOTE: kepler.db and data/processed/*.npz ARE committed on purpose so the
# Streamlit Cloud dashboard works immediately on first visit (bundled demo
# data: the synthetic targets + Kepler 8-17 + TOI-132).
