# Dashboard theme — a deep-space dark palette with a transit-blue accent.
# Streamlit reads appearance from here; no CSS injection needed.
[theme]
base = "dark"
primaryColor = "#4da6ff"
backgroundColor = "#0b0f1a"
secondaryBackgroundColor = "#161c2e"
textColor = "#e6ebf5"
